plugins {
    id 'java-library'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

dependencies {
    // utilities 모듈에서 외부 라이브-리 쓰면 여기에 추가
    implementation project(':list')
}